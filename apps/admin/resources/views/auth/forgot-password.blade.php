<!DOCTYPE html>
<html lang="pt-BR" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Turni · Backoffice — Recuperar senha</title>
    <meta name="robots" content="noindex,nofollow">
    <style>
        :root {
            --page: #F7F4EC; --surface: #FFFFFF; --border: #E0DDD3;
            --text: #0F1B2D; --text-muted: #42504A; --accent: #2A4D8F;
            --on-accent: #FFFFFF; --accent-hover: #21407A;
            --radius-lg: 16px; --radius-full: 999px;
        }
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: 'Inter', -apple-system, sans-serif; background: var(--page); color: var(--text); min-height: 100vh; display: flex; align-items: center; justify-content: center; }
        .card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-lg); padding: 40px; width: 100%; max-width: 420px; box-shadow: 0 1px 2px rgba(15,27,45,.04), 0 8px 24px rgba(15,27,45,.06); }
        .logo { font-family: 'Bebas Neue', 'Impact', sans-serif; font-size: 24px; letter-spacing: 2px; color: var(--text); }
        .logo .i { color: #00A868; }
        .tag { font-family: monospace; font-size: 10px; letter-spacing: 1.5px; text-transform: uppercase; color: var(--text-muted); margin-top: 4px; }
        hr { border: none; border-top: 1px solid var(--border); margin: 20px 0; }
        h1 { font-size: 18px; margin-bottom: 10px; }
        p { font-size: 14px; color: var(--text-muted); line-height: 1.5; }
        a.btn { display: inline-block; margin-top: 24px; padding: 12px 28px; background: var(--accent); color: var(--on-accent); border-radius: var(--radius-full); font-size: 14px; font-weight: 500; text-decoration: none; }
        a.btn:hover { background: var(--accent-hover); }
    </style>
</head>
<body>
    <div class="card">
        <div class="logo">TURN<span class="i">I</span>.</div>
        <div class="tag">Backoffice</div>
        <hr>
        <h1>Recuperação de senha</h1>
        <p>O envio do link de redefinição por e-mail chega na STORY-021. Por
           enquanto, fale com o administrador do sistema para redefinir sua senha.</p>
        <a class="btn" href="/login">Voltar ao login</a>
    </div>
</body>
</html>
